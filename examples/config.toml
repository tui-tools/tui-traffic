# tui-traffic configuration.
#
# Read from /etc/tui-traffic/config.toml first, then
# ~/.config/tui-traffic/config.toml, with the user file overriding the
# machine-wide one. TUI_TRAFFIC_* environment variables override both, and
# command line flags override everything.

# How often to sample. A rate is the difference between two readings and the
# time between them, so this is the window every number on the interfaces
# screen is measured over. It is held between 100ms and 30s.
interval = "1s"

# Privilege escalation prefix, used for the one read that needs root: the
# connection tracking table. "" runs without escalation, and the connections
# screen then falls back to counting sockets and says so.
sudo = "sudo -n"

# Path to an Omarchy-style colors.toml. Empty follows the active Omarchy
# theme when present, and falls back to the built-in Tokyo Night palette.
theme = ""

# Every key can also be set in the environment, upper-cased and prefixed:
#   TUI_TRAFFIC_INTERVAL=2s
#   TUI_TRAFFIC_SUDO="doas"
